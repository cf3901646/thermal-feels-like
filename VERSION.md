# 版本存档记录

| 版本号 | 存档时间 | 描述 | 状态 |
| :--- | :--- | :--- | :--- |
| **v3.0** | 2026-05-19 00:03 | **中国高精城市数据库扩容版**：全量集成 3200+ 个中国省市、县级行政区、区县（如溧阳市等），实现 0ms 离线高精模糊检索与定位；彻底移除对 Nominatim 的依赖，对于本地未命中的其他地区统一采用 Open-Meteo Geocoding 作为开源友好的无 Key 中英文兜底方案，完美契合开源项目发布标准。 | **当前版本** |
| **v2.3** | 2026-05-18 23:02 | **UI与搜索体验及色彩精调版**：拦截国内城市联想导致的海外地名乱入；恢复图表X轴多行标签支持；修复浅色背景下状态栏图标不可见问题；精调双图表低温曲线配色（室外低温采用麦芽沙色 #CCA990，室内低温采用温柔雾霾蓝 #92A8B9）。 | **备份 (稳定)** |
| **v2.2** | 2026-05-18 18:18 | **混合搜索与离线反查重构版**：设计并集成了本地 400+ 个中国核心城市的紧凑数据库。重构城市检索与逆地理反查逻辑，实现国内城市拼音/简拼检索 0ms 秒展，地图点击定位 0ms 最近邻离线地名反查；针对国外城市自动合并 Open-Meteo 接口以及 3s 超时 Nominatim 网络兜底。 | **备份 (稳定)** |
| **v2.1** | 2026-05-15 10:01 | **UI 视觉统一版**：将全站高亮标注背景统一为 #F7F7F7，并将详情格与逐小时高亮的圆角修正为 28px 以对齐整体规范。 | **备份 (稳定)** |
| **v2.0** | 2026-05-15 09:33 | **正式里程碑版 (基于 APK-17)**：由凌晨最后一个稳定 APK 提取并确定的基准版本。 | **备份 (稳定)** |
| **v1.30** | 2026-05-15 09:21 | **版本回滚**：根据用户要求，已将 Web 端及 Android 原生端代码整体回滚至昨天的 **v1.25 (诊断版)**。 | **备份 (稳定)** |
| **v1.29** | 2026-05-15 09:10 | **性能调优与轻量化版**：移除高耗能的 `backdrop-filter` 高斯模糊效果，改用轻量级透明底色。增加 `resize` 防抖处理，精简滚动重置逻辑，显著提升移动端流畅度。 | **备份 (稳定)** |
| **v1.28** | 2026-05-15 08:55 | **历史回溯平衡版**：调整默认滚动位置，使“现在”居于视图第二项（左侧预留一项历史数据）。优化了滚动偏移量计算公式。 | **备份 (稳定)** |
| **v1.27** | 2026-05-15 08:52 | **UI 视觉重构版**：设计了全新的“现在”项高亮背景。将“现在”强制设为列表起始位置。确立了项目备份与同步的工作规范。 | **备份 (稳定)** |
| **v1.26** | 2026-05-15 08:45 | **滚动归位攻坚版**：通过 `MutationObserver` 与阶梯式定时器解决了 Android WebView 渲染异步导致的滚动条复位失败问题。 | **备份 (稳定)** |
| **v1.25** | 2026-05-14 23:22 | **全方位诊断与可视化探测版**：集成了 Java 层循环注入、JS 层智能轮询、以及前端可视化调试面板。用于实时监测 Native、CSS、JS 三方数据的传递状态，彻底排查适配失效的物理链路。 | **备份 (稳定)** |
| **v1.24** | 2026-05-14 23:20 | **原生高度强行注入版**：初版注入逻辑（诊断升级中）。 | **备份 (稳定)** |
| **v1.23** | 2026-05-14 23:15 | **GitHub 级原生补丁适配版**：开启 Insets 传递版（已升级）。 | **备份 (稳定)** |
| **v1.22** | 2026-05-14 23:10 | **像素密度补偿适配版**：逻辑像素换算版（已升级）。 | **备份 (稳定)** |
| **v1.21** | 2026-05-14 23:05 | **原生接口高度适配版**：调用 `StatusBar.getHeight()` 进行动态适配。 | **备份 (稳定)** |
| **v1.20** | 2026-05-14 23:02 | **异形屏状态栏兼容性增强版**：暴力提升保底值版（已弃用）。 | **备份 (稳定)** |
| **v1.19** | 2026-05-14 22:58 | **图表间距全局对齐版**：对齐室内外体感间距。 | **备份 (稳定)** |
| **v1.18** | 2026-05-14 22:56 | **REM 联动及紧凑对齐版**：重构悬浮栏单位并精调间距。 | **备份 (稳定)** |
| **v1.17** | 2026-05-14 22:55 | **视觉边缘对齐修正版**：考虑到定位栏容器的 0.4rem 内边距进行修正。 | **备份 (稳定)** |
| **v1.16** | 2026-05-14 22:50 | **全 1.2rem 绝对等距版**：按照用户要求对齐间距。 | **备份 (稳定)** |
| **v1.15** | 2026-05-14 22:45 | **最终比例对齐版**：参照页面板块间距（1.2rem）进行了终极精调。 | **备份 (稳定)** |
| **v1.14** | 2026-05-14 22:42 | **悬浮窗视觉精调版**：下移悬浮窗位置（+1.2rem）以避开状态栏。 | **备份 (稳定)** |
| **v1.13** | 2026-05-14 22:30 | **终极悬浮修正版**：将定位模块彻底移出 `app-container` 容器，直接挂载于 `body` 下。通过解除父级容器的布局约束，实现了绝对的 `fixed` 定位，确保定位栏在任何滑动情况下均始终固定在视口顶部。 | **备份 (稳定)** |
| **v1.12** | 2026-05-14 22:20 | **悬浮窗定位模块版**：将地理位置管理中心改为 `fixed` 悬浮定位，利用 `pointer-events: none` 实现点击穿透。 | **备份 (稳定)** |
| **v1.11** | 2026-05-14 15:22 | **智能安全高度嗅探版**：抛弃了一刀切的 48px 硬保底。通过 JS 实时嗅探 `env()` 有效性，成功返回则按需适配，失效则回退至 28px 温和保底。实现了兼顾“不撞头”与“不空洞”的优雅视觉平衡。 | **备份 (稳定)** |
| **v1.10** | 2026-05-14 15:20 | **顶部避让与间距增广版**：针对部分手机 `env` 变量失效问题，强制设置 48px 顶部保底高度，确保定位栏不被状态栏遮挡。同时将全局板块间距拉大至 1.2rem，提升界面呼吸感。 | **备份 (稳定)** |
| **v1.9** | 2026-05-14 15:15 | **局部间距紧凑化版**：针对“室内/室外体感”折线图板块进行了局部归组，将其组内间距收缩为 0.4rem。因为这两个板块自带内部标题，缩短间距后视觉效果更加整体、自然。 | **备份 (稳定)** |
| **v1.8** | 2026-05-14 15:14 | **像素级视觉对齐版**：通过打平 HTML 嵌套层级（移除 main-content 包装），强制所有板块直接接受 `app-container` 的统一间距控制，并重置了标题边距，实现了绝对一致的纵向节奏。 | **备份 (稳定)** |
| **v1.7** | 2026-05-14 15:10 | **视觉规范化 (间距对齐) 版**：清理了各板块之间散乱的 Margin，统一通过容器 `gap` 控制，确保从定位栏到预报列表的所有间距完全一致（0.85rem）。 | **备份 (稳定)** |
| **v1.6** | 2026-05-14 15:08 | **全沉浸式状态栏版**：深度学习墨迹天气方案，启用原生 Overlays 模式，将网页背景冲向顶端。通过 `env(safe-area-inset-top)` 动态感应状态栏高度，实现完美的背景沉浸与文字避让。 | **备份 (稳定)** |
| **v1.5** | 2026-05-14 15:06 | **终极比例精调版**：彻底移除残余的 px 单位（图标 130px -> 7.5rem），微调主气温字号 (3rem -> 2.6rem) 并收缩全局间距。在 100% 保持设计感的同时，确保了极端窄屏下的零溢出显示。 | **备份 (稳定)** |
| **v1.4** | 2026-05-14 15:02 | **REM 比例联动缩放版**：终极适配方案。将 html 根字号与视口宽度 (100vw) 绑定。所有以 rem 为单位的元素（文字、布局、间距）都会随屏幕宽度自动等比伸缩，彻底杜绝溢出。 | **备份 (稳定)** |
| **v1.3** | 2026-05-14 15:01 | **动态缩放引擎版**：彻底抛弃视口缩放，改用 JS 实时计算比例并应用 CSS `zoom`。无论物理屏幕大小，均强制呈现 385px 基准下的完美比例布局。 | **备份 (稳定)** |
| **v1.2** | 2026-05-14 14:59 | **硬核视口锁定版**：改用固定宽度 (385px) 视口，强制所有手机不论屏幕大小和系统设置，均显示相同的 UI 比例。 | **备份 (稳定)** |
| **v1.1** | 2026-05-14 14:53 | **全局缩放锁定版**：锁定 Viewport 缩放和文字自动调节，解决不同手机因系统字体大小设置导致的布局错乱问题。 | **备份 (稳定)** |
| **v1.0** | 2026-05-14 14:50 | 从微信传回的 APK (app-debug(13).apk) 中逆向提取的“完美版本”。包含 3D PNG 图标、左对齐布局、沉浸式状态栏。 | **备份 (稳定)** |
